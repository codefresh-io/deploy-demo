# codefresh yaml file
  version: "1.0"
  steps:
    preb_run_validation:
       image: node:latest
       commands:
         - ls .
         - git status
         - git describe --tags
    build-step:
        type: build
        description: Builds my service
        dockerfile: Dockerfile
        image_name: verchol/deploy-demo
        tag: ${{CF_BRANCH}}

    preb_run_validation:
       image: ${{build-step}}
       commands:
         - node ./index